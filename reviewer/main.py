import pygithub

def review_code(pr, codebase):
    # This function is responsible for reviewing the code
    # It should take each of the PR comments, the associated code chunks and then return updated code that actually fixes the issues 
    # It should update the PR that is passed in and return that diff 
    updatedDiff = ""
    return updatedDiff 

